<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="superp.techpark.ru.lesson8">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="superp.techpark.ru.lesson8.MessageActivity"/>

        <activity android:name="superp.techpark.ru.lesson8.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:exported="false"
            android:enabled="false"
            android:name="superp.techpark.ru.lesson8.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />


        <activity android:name="superp.techpark.ru.lesson8.IntentServiceActivity"></activity>

        <service
            android:name="superp.techpark.ru.lesson8.DemoIntentService"
            android:exported="false"></service>

    </application>
</manifest>
